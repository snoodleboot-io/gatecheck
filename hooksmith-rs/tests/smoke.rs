//! Smoke test — confirms the crate compiles and links its dependencies.
//! Module-level integration tests go in their own files alongside this one.

#[test]
fn crate_compiles() {
    // Intentionally minimal. Real tests land as each module is implemented.
}
